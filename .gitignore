# dependencies
node_modules
.pnp
.pnp.js

# build
dist
dist-ssr
build
*.local

# env
.env
.env.local
.env.*.local

# logs
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*

# editor
.vscode/*
!.vscode/extensions.json
!.vscode/settings.json
.idea
*.swp
*.swo
.DS_Store

# misc
*.tsbuildinfo
coverage
.eslintcache

# Per-developer Claude config + harness worktrees
# (matches sibling project kalku-procurement — .claude/ is personal, not shared)
.claude/

# API service data + env
/api/node_modules/
/api/data/
/api/.env
/api/.env.local

# Panel API (Hono + SQLite) — same conventions
/panel-api/node_modules/
/panel-api/data/
/panel-api/dist/
/panel-api/.env
/panel-api/.env.local
# nested-cwd quirk where Playwright's webServer cwd makes the data dir
# materialize at panel-api/panel-api/data/. Belt-and-braces.
/panel-api/panel-api/

# Playwright artifacts (test-results + the HTML report). Per-test screenshots
# we want kept go in docs/v2_redesign/e2e/.
/test-results/
/playwright-report/
/docs/v2_redesign/e2e/playwright-report/
